import type { PlatformAPI } from '@game-core/platform/types';

export function createDouyinPlatform(): PlatformAPI {
  throw new Error('Douyin adapter not implemented yet. Add tt-specific input, storage, audio, and lifecycle wiring here.');
}
